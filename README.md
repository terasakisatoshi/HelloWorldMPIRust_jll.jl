# `HelloWorldMPIRust_jll.jl` (v0.1.0+0)

This is an autogenerated package constructed using [`BinaryBuilder.jl`](https://github.com/JuliaPackaging/BinaryBuilder.jl).

## Documentation

For more details about JLL packages and how to use them, see `BinaryBuilder.jl` [documentation](https://docs.binarybuilder.org/stable/jll/).

## Sources

The tarballs for `HelloWorldMPIRust_jll.jl` have been built from these sources:

* files in directory, relative to originating `build_tarballs.jl`: `bundled`
* git repository: https://github.com/rsmpi/rsmpi.git (revision: `5487f7f6733397b3e672e52696c26f382725036d`)
* git repository: https://github.com/libffi-rs/libffi-rs.git (revision: `b687efab739d0f02050fe536ac4b1bafd467d4f2`)

## Platforms

`HelloWorldMPIRust_jll.jl` is available for the following platforms:

* `Platform("aarch64", "macos"; mpi = "mpich")` (`aarch64-apple-darwin-mpi+mpich`)
* `Platform("aarch64", "macos"; mpi = "mpitrampoline")` (`aarch64-apple-darwin-mpi+mpitrampoline`)
* `Platform("aarch64", "macos"; mpi = "openmpi")` (`aarch64-apple-darwin-mpi+openmpi`)
* `Platform("aarch64", "linux"; mpi = "mpich", libc = "glibc")` (`aarch64-linux-gnu-mpi+mpich`)
* `Platform("aarch64", "linux"; mpi = "mpitrampoline", libc = "glibc")` (`aarch64-linux-gnu-mpi+mpitrampoline`)
* `Platform("aarch64", "linux"; mpi = "openmpi", libc = "glibc")` (`aarch64-linux-gnu-mpi+openmpi`)
* `Platform("x86_64", "linux"; mpi = "mpich", libc = "glibc")` (`x86_64-linux-gnu-mpi+mpich`)
* `Platform("x86_64", "linux"; mpi = "mpitrampoline", libc = "glibc")` (`x86_64-linux-gnu-mpi+mpitrampoline`)
* `Platform("x86_64", "linux"; mpi = "openmpi", libc = "glibc")` (`x86_64-linux-gnu-mpi+openmpi`)
* `Platform("x86_64", "freebsd"; mpi = "mpich")` (`x86_64-unknown-freebsd-mpi+mpich`)
* `Platform("x86_64", "freebsd"; mpi = "mpitrampoline")` (`x86_64-unknown-freebsd-mpi+mpitrampoline`)
* `Platform("x86_64", "freebsd"; mpi = "openmpi")` (`x86_64-unknown-freebsd-mpi+openmpi`)
* `Platform("x86_64", "windows"; mpi = "microsoftmpi")` (`x86_64-w64-mingw32-mpi+microsoftmpi`)

## Dependencies

The following JLL packages are required by `HelloWorldMPIRust_jll.jl`:

* `CompilerSupportLibraries_jll`
* `MPICH_jll`
* `MPIPreferences`
* `MPItrampoline_jll`
* `MicrosoftMPI_jll`
* `OpenMPI_jll`

## Products

The code bindings within this package are autogenerated from the following `Products`:

* `ExecutableProduct`: `hello_world`
